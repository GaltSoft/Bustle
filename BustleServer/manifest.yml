declared-services:
  TodoListCloudantDatabase:
    label: cloudantNoSQLDB
    plan: Lite
applications:
- name: BlusterCloudantApp
  memory: 256M
  instances: 1
  random-route: true
  command: Server
  services:
  - BlusterCloudantDatabase

